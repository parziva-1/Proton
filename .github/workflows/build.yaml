name: "Build"

on:
  push:
    branches: ["proton-new"]

jobs:
  ajob:
    runs-on: ubuntu-latest
    steps:
       - name: Action
         uses: actions/checkout@v4

         runs: export WP="$(realpath $PWD/../)"
         runs: . build/ckbuild_actions.sh -tkR
         runs: . build/ckbuild_actions.sh -tR

  bruh:
    runs-on: ubuntu-latest
    steps:
       - run: echo "Wasting Microsoft resources ig?"