name: "Build"

on:
  push:
    branches: ["proton-new"]

jobs:
  ajob:
    runs-on: ubuntu-latest
    steps:
       - name: Action
         uses: action/checkout@v4

       - name: Env
         run: . build/setwp.sh
       - name: Build KSU
         run: . build/ckbuild_actions.sh -tkR
       - name: Build
         run: . build/ckbuild_actions.sh -tR

  bruh:
    runs-on: ubuntu-latest
    steps:
       - run: echo "Wasting Microsoft resources ig?"